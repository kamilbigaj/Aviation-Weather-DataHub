aviation_analytics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('RDS_HOST') }}"
      user: "{{ env_var('RDS_USER') }}"
      password: "{{ env_var('RDS_PASSWORD') }}"
      port: "{{ env_var('RDS_PORT') | int }}"
      dbname: postgres
      schema: public
      threads: 4